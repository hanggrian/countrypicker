buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
        classpath 'com.novoda:bintray-release:0.3.4'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

ext {
    compileSdkVersion = 24
    buildToolsVersion = '24.0.3'
    minSdkVersion = 11
    targetSdkVersion = 24

    supportLibraryVersion = '24.2.1'
    fastScrollVersion = '1.0.11'

    bintrayUserOrg = 'hendraanggrian'
    bintrayGroupId = 'io.github.hendraanggrian'
    bintrayArtifactId = 'countrypickerdialog'
    bintrayPublishVersion = '0.1.0'
    bintrayDesc = 'Material design dialog to pick country.'
    bintrayWebsite = 'https://github.com/hendraanggrian/countrypickerdialog'
}

//$ ./gradlew bintrayUpload -PbintrayUser=hendraanggrian -PbintrayKey= -PdryRun=false